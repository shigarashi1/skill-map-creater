import { GridSize } from '@material-ui/core/Grid';
import { IndexesObject } from '../types';

export const SAMPLE_PAGE_BREAK_POINT: IndexesObject<GridSize> = {
  xs: 12,
  sm: 6,
  md: 4,
  lg: 3,
};
